[project]
name = "udacity-mba-in-ai-product-management"
version = "0.1.0"
description = "Markdown-first study workspace with an optional MkDocs site layer."
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material",
]

[tool.uv]
package = false
